# General Image Example

Task:

- Create a clean product-style image of a glass teapot on a rainy windowsill.
- No references.

Contract:

- Preserve: requested subject, transparent glass, warm tea color, clean hierarchy.
- Change: create the scene, composition, and lighting from user text.
- Ignore: random text, logos, watermarks, UI, unrelated objects, hidden references.

Expected debug check:

- `reference_interpretation.md` says no references supplied.
- `quality_preflight.md` lists task type and PCI before scene details.
