# Reference Guided Image Template

Use when one or more reference images should guide only declared dimensions.

Rules:

- Every reference must include `path` and one formal `role`.
- Reference order has no meaning.
- User text wins over unassigned source details.
- Preserve / Change / Ignore must be written before scene description.
