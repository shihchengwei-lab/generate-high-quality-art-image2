# General Image Template

Use for a pure text Image 2.0 request with no reference images.

Required contract:

- `task_type: general_image`
- `reference_images: []`
- `preserve`
- `change`
- `ignore`

Keep the prompt focused on the requested subject, output form, composition, scene, style, and clean-render constraints.
